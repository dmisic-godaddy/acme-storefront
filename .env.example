# Port the Express server listens on
PORT=3000

# Shared secret for verifying webhook subscription signatures (HMAC-SHA256).
# Used by the orderCreated handler. Get this from the developer portal.
# Leave blank to skip signature verification during early local testing.
GODADDY_WEBHOOK_SECRET=
